def get_db():
    return mysql.connector.connect(
        host="localhost",
        user="sa",            # แก้ให้ตรงกับ MySQL ของคุณ
        password="sa",    # แก้ให้ตรงกับ MySQL ของคุณ
        database="roomsdb"      # ชื่อฐานข้อมูลที่สร้างไว้
    )